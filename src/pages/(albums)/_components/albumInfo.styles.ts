import styled from "styled-components";
import tw from "twin.macro";

export const AlbumInfoBox = styled.div`
  display: flex;
  flex-direction: column;
  overflow-x: hidden;

  ${tw`row-start-1 row-span-3 sm:justify-center mb-4`}
`;
